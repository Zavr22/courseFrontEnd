interface IProjector {
  id: string;
  name: string;
  category_id: number;
  quantity: number;
  brightness: string;
  contrast: string;
  price: number;
  weight: number;
  focal_distance: number;
}

export default IProjector;
